name: REFINE_nano
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.8.20
  - pip
  - pip:
    - tensorflow==1.15.5        # TF1 for aarch64, use pre-built wheel if available
    - gym==0.26.2
    - opencv-python==4.11.0.86
    - ray>=2.2.0
    - numpy<1.24                # TF1 compatibility
    - scipy
    - matplotlib
    - configdict
